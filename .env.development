# Disable telemetry for faster startup
NEXT_TELEMETRY_DISABLED=1

# Enable SWC for faster compilation
NEXT_SWC_MINIFY=true

# Limit verbose logging
NEXT_DISABLE_VERBOSE=1

# Increase memory limit if needed (adjust based on your system)
NODE_OPTIONS=--max-old-space-size=4096 